# F-002 — Organize Production sync by sprint

- **Status:** closed — 2026-08-30
- **Decision:** every `Production sync` sprint is one named horizontal Figma
  section. Frames progress left-to-right according to that sprint's actual sync
  phase: baseline/evidence, comparison or decision, current Figma authority,
  then implementation/runtime evidence when it exists.
- **Operating correction:** identify the sprint and phase before creating or
  moving a frame. Place it inside the sprint section with a consistent gap and
  phase-revealing name; resize the section to contain all frames. Do not add
  page-level frames or global phase sections for new work.
- **Close when:** the existing `Production sync` page is migrated into named
  horizontal sprint sections, its frames have an unambiguous left-to-right sync
  order, and a page-level visual/hierarchy review records the result.
- **Resolution:** the page now contains only three named sprint sections:
  `Sprint 01 · Cases baseline and target` (`211:3`), `Sprint 02 ·
  Available-times browser` (`251:21`), and `Sprint 03 · In-call capture`
  (`264:21`). The former global phase sections and the separate scheduling
  archive were removed only after every child frame was preserved and moved.
- **Verification:** hierarchy and rendered-frame review confirmed eight frames
  remain, each sprint is a horizontal band, and the order is clear: Cases
  evidence → reconstruction → decision → target; scheduling history → current
  authority; and In-call A → B → C.
